{% extends 'base.html.twig' %}

{% block content %}
<div class="container">
    <div class="row">
        <div class="col-sm-12">
           <h3>Articles</h3>
            <h4>rand num : {{rand_num}}</h4>
            <h2>{% if user %}Witaj! {{user['first_name']}}{% else %}not logged{% endif %}</h2>
        </div>

    </div>
</div>
{% endblock %}